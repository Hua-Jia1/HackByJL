# 网络运维工程师入厂标准操作程序（SOP）

> **版本**：v1.0
> **适用**：首次入驻新环境/接手新网络的网络运维工程师
> **目标**：用一套可重复的步骤，在 **7 天内**完成环境摸底、建立基线、消除明显隐患

---

## 目录

- [第 0 步：准备工作](#第-0-步准备工作)
- [第 1 步：拿到"四件套"](#第-1-步拿到四件套)
- [第 2 步：搭建工作环境](#第-2-步搭建工作环境)
- [第 3 步：设备信息采集](#第-3-步设备信息采集)
- [第 4 步：物理与逻辑梳理](#第-4-步物理与逻辑梳理)
- [第 5 步：监控/告警/日志接入](#第-5-步监控告警日志接入)
- [第 6 步：建立配置备份机制](#第-6-步建立配置备份机制)
- [第 7 步：风险排查与隐患清单](#第-7-步风险排查与隐患清单)
- [第 8 步：写交接手册](#第-8-步写交接手册)
- [第 9 步：第一次月度回顾](#第-9-步第一次月度回顾)
- [第 10 步：按厂商速查与命令详解](#第-10-步按厂商速查与命令详解)
- [附录 A：推荐软件清单](#附录-a推荐软件清单)
- [附录 D：通用项目手册模板](#附录-d通用项目手册模板)
- [附录 B：常用账户/资产表模板](#附录-b常用账户资产表模板)
- [附录 C：变更流程与回滚模板](#附录-c变更流程与回滚模板)

---

## 总览图

### 整体网络架构

```mermaid
graph TB
    subgraph ISP["互联网"]
        Internet((Internet))
    end

    subgraph TwoFloor["二楼核心机房（华为）"]
        AR6300[AR6300-S 核心路由器]
        USG[USG6000E 防火墙]
        S5735In[S5735 内网核心]
        S5735Out[S5735 外网核心]
        R740[R740 叫号服务器]
    end

    subgraph OneFloor["一楼业务网（锐捷+华三）"]
        EG3230[EG3230 外网网关]
        F1000[F1000-AK155 防火墙]
        EG3220[EG3220 内网网关]
        S5760C1[S5760C 核心1]
        S5760C2[S5760C 核心2]
        S5760C3[S5760C 核心3]
        S5310A[S5310 接入1]
        S5310B[S5310 接入2]
        S5310C[S5310 接入3]
        S5310D[S5310 接入4]
        WS6008[WS6008 无线AC]
    end

    subgraph Terminal["终端/业务"]
        PC[办公PC]
        AP[无线AP]
        SIP[深信服终端]
        HIK[海康摄像头]
    end

    Internet --> EG3230
    EG3230 --> F1000
    F1000 --> S5760C1
    S5760C1 <--> S5760C2
    S5760C2 <--> S5760C3
    S5760C1 --> EG3220
    S5760C1 --> S5310A
    S5760C1 --> S5310B
    S5760C1 --> S5310C
    S5760C1 --> S5310D
    S5760C1 --> WS6008

    S5760C1 -.互联.- S5735In
    S5735In --> S5735Out
    S5735Out --> USG
    USG --> AR6300
    AR6300 --> Internet
    S5735In --> R740

    S5310A --> PC
    WS6008 --> AP
    EG3220 --> SIP
    S5310A --> HIK

    style TwoFloor fill:#e1f5ff
    style OneFloor fill:#fff4e1
    style ISP fill:#f0f0f0
    style Terminal fill:#e8f5e9
```

### 入厂 7 天时间线

```mermaid
gantt
    title 入厂 7 天时间线
    dateFormat YYYY-MM-DD
    axisFormat %m-%d

    section Day 1 入厂
    拿四件套（资产/拓扑/IP/账号）        :a1, 2026-06-11, 1d
    申请所有系统账号                    :a2, after a1, 1d
    装好工具链                          :a3, after a1, 1d
    机房走一遍                          :a4, after a2, 1d

    section Day 2-3 采集
    跑批量采集脚本                       :b1, after a4, 1d
    分析采集结果（错包/时钟/邻居）       :b2, after b1, 1d
    登记隐患到清单                      :b3, after b2, 1d

    section Day 4-5 拓扑与备份
    画现状拓扑图（对比文档）             :c1, after b3, 1d
    部署 Oxidized / 备份脚本            :c2, after c1, 1d
    验证备份可恢复                      :c3, after c2, 1d

    section Day 6-7 监控与文档
    把所有设备加监控                    :d1, after c3, 1d
    NTP / syslog 集中                   :d2, after d1, 1d
    写完资产清单 / IP 规划              :d3, after d2, 1d
    写"如果我请假一个月"手册            :d4, after d3, 1d
```

### 入厂流程泳道

```mermaid
flowchart LR
    A[入厂报到] --> B[拿四件套]
    B --> C[申请账号]
    C --> D[机房巡检]
    D --> E[设备信息采集]
    E --> F{配置与文档<br/>是否一致?}
    F -->|不一致| G[问清楚/登记隐患]
    F -->|一致| H[画现状拓扑]
    G --> H
    H --> I[部署自动备份]
    I --> J[接入监控]
    J --> K[写交接手册]
    K --> L[月度回顾]

    style A fill:#e3f2fd
    style L fill:#c8e6c9
    style G fill:#ffcdd2
    style F fill:#fff9c4
```

### 配置备份架构

```mermaid
flowchart LR
    subgraph Devices["网络设备（15 台）"]
        D1[Ruijie 设备]
        D2[Huawei 设备]
        D3[H3C 设备]
    end

    subgraph Backup["备份服务器（建议 Linux）"]
        OX[Oxidized]
        GIT[(Git 仓库)]
        CRON[定时任务]
    end

    subgraph Notify["告警"]
        MAIL[邮件]
        SLACK[Slack/钉钉]
    end

    D1 -->|SSH 每24h| OX
    D2 -->|SSH 每24h| OX
    D3 -->|SSH 每24h| OX
    OX -->|commit| GIT
    OX -->|配置变更| MAIL
    OX -->|配置变更| SLACK
    CRON -->|调度| OX

    style Devices fill:#fff4e1
    style Backup fill:#e1f5ff
    style Notify fill:#ffebee
```

---

## 第 0 步：准备工作

入厂前/入厂第一天先准备好这些东西，能省你后面 80% 的麻烦。

### 必须带的物品

- 笔记本电脑（推荐 16G 内存 + 512G SSD，运维要跑的东西不少）
- Console 线 × 2（USB 转 RJ45 + 传统 RJ45 转 USB，最好各带一条）
- USB 转串口驱动（CH340 / FT232 芯片都要支持）
- 螺丝刀（十字 + 一字 + 6 角套筒，机柜门/U 位固定）
- 手电筒（机房普遍偏暗）
- 标签纸 + 油性笔（标注线缆、U 位）
- 小本子 + 多支笔
- U 盘（2 个以上，备份用）

### 账号与权限申请

入厂当天就找 IT 主管开这些账号：

| 账号类型 | 用途 | 紧急程度 |
|---------|-----|---------|
| 网络设备 SSH/Console 账号 | 看配置、改配置 | ⭐⭐⭐ |
| 堡垒机/跳板机 | 集中登录审计 | ⭐⭐⭐ |
| 服务器/虚拟化平台 | vCenter / iDRAC | ⭐⭐⭐ |
| 监控平台 Admin | Zabbix / PRTG / 其它 | ⭐⭐ |
| 防火墙/AC Web 控制台 | 图形化管理 | ⭐⭐ |
| 域名/SSL 证书管理 | 业务侧 | ⭐ |
| OA/工单系统 | 变更审批 | ⭐⭐⭐ |
| 业务对接人微信/电话 | 紧急联系 | ⭐⭐⭐ |

---

## 第 1 步：拿到"四件套"

跟前任/主管要下面这 4 份东西，**拿不到就先别动配置**。

### 1. 资产清单

需要包含：
- 设备型号、序列号、采购日期、维保截止日期
- 物理位置（机房、机柜、U 位）
- 管理 IP、Console 口位置
- 上联/下联关系

模板在 `06-资产与拓扑/01-资产清单模板.md`

### 2. 网络拓扑图

要 **L2 和 L3 两份**：
- L2：VLAN 划分、Trunk 链路、生成树
- L3：IP 网段、互联地址、路由协议

> **重要**：文档里的拓扑和现实很可能不一样，**要自己核对**。

### 3. IP 地址规划表

包含：
- 业务网段（用户 VLAN、服务器 VLAN）
- 互联地址（设备之间互联用）
- Loopback 地址
- 公网 IP / NAT 映射
- 保留网段

模板在 `06-资产与拓扑/02-IP规划表模板.md`

### 4. 账号表

包含：
- 设备本地账号
- 堡垒机账号
- Web 控制台账号
- 各级密码（按规范应放密码管理器，不应明文保存）

> ⚠️ 密码建议用 **1Password / KeePass / Bitwarden** 之类的工具管，别明文存电脑或文档里。

---

## 第 2 步：搭建工作环境

### 推荐软件清单（详见附录 A）

按"必备 → 推荐 → 选装"三档分类。

#### 必备（不装没法干活）

| 软件 | 用途 | 推荐 |
|------|------|------|
| 终端工具 | SSH / Telnet / Console | **MobaXterm**（Windows 神器，SSH + 串口 + 文件传输 + 多标签） |
| 串口驱动 | USB 转 Console | CH340 / FT232 驱动 |
| 文本编辑器 | 看配置、写文档 | **VS Code**（装 SSH + Markdown 插件） |
| 密码管理 | 存账号密码 | **Bitwarden**（开源免费） / 1Password |
| 抓包工具 | 排错 | **Wireshark** |
| 远程桌面 | 跳板/服务器 | Windows 自带 mstsc |

#### 推荐（强烈建议装）

| 软件 | 用途 |
|------|------|
| NetBox | IP 资产管理 + 拓扑（自建，免费） |
| Zabbix / Prometheus | 监控告警（自建） |
| Oxidized / RANCID | 自动备份配置（自建） |
| Draw.io / Visio | 画拓扑图 |
| Xshell + Xftp | 备选终端工具 |
| SecureCRT | 备选终端（老牌稳） |
| Snipaste | 截图贴图（写文档神器） |
| Everything | 文件搜索 |

#### 选装（按需要）

| 软件 | 用途 |
|------|------|
| Solar-Putty | 轻量 SSH 客户端 |
| Termius | 跨平台 SSH |
| 网络管理平台 | 华为 eSight / 锐捷 RIIL / H3C iMC |
| PRTG / LibreNMS | 商业 / 开源监控 |
| Docusaurus / MkDocs | 内网 Wiki |

### 跳板/堡垒机配置

大多数公司有堡垒机，没有的话自己搭一个 **Jumpserver**（开源免费）。

- 第一周内完成跳板机登录验证
- 所有 SSH 通过跳板机，记录审计
- 关键操作二次授权

---

## 第 3 步：设备信息采集

> **核心原则：能采就采，能存就存，所有信息都要落地到本地。**

### 3.1 准备采集清单

把 `02-设备操作手册/` 下每台设备的命令清单过一遍，整理成一张执行表。

### 3.2 用脚本批量采集（强烈推荐）

不要一台台手敲，用 Python + Netmiko 批量跑。

**脚本位置**：`03-自动化脚本/01-批量采集设备配置.py`

执行：
```bash
pip install netmiko
# 编辑脚本里的设备清单和账号
python 01-批量采集设备配置.py
```

结果会按设备分文件保存到 `04-配置备份/采集结果-YYYYMMDD/`。

### 3.3 手敲命令（脚本跑不通的设备）

按设备类型打开对应的手册，挨个敲命令，输出复制保存。

### 3.4 采集完成后必看的几项

- [ ] **时钟准不准**（`show clock` / `display clock`），差太多的话日志对不上
- [ ] **接口有没有 err/discard/crc 错包**（`show interface counters`）
- [ ] **running-config 和 startup-config 一不一致**（不一致 = 之前有人改完没保存，是个雷）
- [ ] **HSRP/VRRP/双机/聚合有没有单挂**（`show standby` / `display vrrp` / `show aggregateport`）
- [ ] **路由邻居有没有起来**（`show ip ospf neighbor` / `display ospf peer`）
- [ ] **CPU/内存有没有长期高负载**（`show cpu` / `display cpu-usage`）

把异常项登记到 `05-变更流程与记录/02-入厂隐患清单.md`。

---

## 第 4 步：物理与逻辑梳理

### 4.1 物理巡检

进机房（**带手电、戴静电环**），对每台设备做：

- [ ] 设备在哪个机柜、几 U
- [ ] 设备前面板灯：SYS/PWR/LINK 灯状态（**拍照**）
- [ ] 电源：单电源还是双电源，分别接哪路 PDU
- [ ] Console 线：接哪台笔记本/堡垒机
- [ ] 上联/下联光纤/SFP 模块型号、剩余几根备用
- [ ] 风扇/温度（用红外测温枪，**电源和 SFP 模块位置**）
- [ ] U 位标签、线缆标签是否清晰（**不清的当场补**）

巡检记录表模板：`06-资产与拓扑/03-物理巡检记录模板.md`

### 4.2 画现状拓扑

打开 Draw.io / Visio，**照着设备实际跑出来的结果**画：

- L2：交换机、VLAN、Trunk、生成树
- L3：路由器、防火墙、互联地址、路由协议
- 标注 IP、端口、链路类型

**一定要和文档对比**，差异点列出来，问清楚为什么。

### 4.3 整理互联矩阵

每个设备列出"我接了谁、对端是谁、什么 VLAN/IP/链路类型"。

---

## 第 5 步：监控/告警/日志接入

> 没监控的网络是瞎子网络，**第一周必须把监控接上**。

### 5.1 监控平台

- 已有 Zabbix/PRTG：用现成的，把所有设备加入监控
- 没有任何监控：自建 Zabbix 或 LibreNMS（详见附录 A）

关键监控项：
- 设备存活（ICMP / SNMP）
- CPU / 内存 / 温度
- 接口流量 / 错包 / 丢包
- 上下联链路状态
- 关键业务端口 UP/DOWN
- 设备重启 / 配置变更

### 5.2 日志

- 设备本地日志：`display logbuffer` / `show logging`
- 远端日志：用 syslog 服务器集中收（Kiwi Syslog / Graylog / ELK）
- 时间同步：NTP，所有设备必须同步到同一时钟源

### 5.3 告警

设置告警分级：
- **P0**（立即响应）：核心设备宕机、出口中断、关键业务不通
- **P1**（30 分钟内）：单链路中断、备用资源耗尽
- **P2**（工作时间）：性能指标异常、错包率升高
- **P3**（次日处理）：告警风暴、可疑配置

---

## 第 6 步：建立配置备份机制

> **没备份别动配置，动了配置没备份是找死。**

### 6.1 手动备份

每台设备采集后立刻手动备份：
- 本地：`copy running-config startup-config`（或 `write` / `save`）
- 远端：TFTP / SCP / FTP，保存到 `04-配置备份/`

### 6.2 自动备份

**强烈推荐自建 Oxidized**（开源，免费，支持 70+ 厂商）：

配置文件：`03-自动化脚本/02-Oxidized配置示例.rb`

执行后：
- 每台设备每天自动备份
- 配置变更自动 diff，邮件/Slack 通知
- 历史版本保留 90 天

**没有 Oxidized 的备选方案**：
- RANCID（老牌）
- 简单 cron + Python + Netmiko 脚本（项目里有示例）

### 6.3 备份检查

- 每周抽 3 台设备，看备份文件大小、修改时间
- 每月做一次"恢复演练"：找台旧设备，把备份灌进去，看能不能启动

---

## 第 7 步：风险排查与隐患清单

第一周内必须排查的 **10 大风险**：

| # | 风险 | 排查方法 | 优先级 |
|---|------|---------|--------|
| 1 | 设备时差不一致 | `show clock` 对比 | P1 |
| 2 | running/startup 不一致 | 查采集结果 | P1 |
| 3 | 接口错包/CRC 长期高 | `show interface counters` | P1 |
| 4 | 冗余协议单挂（HSRP/VRRP/聚合/STP） | `show standby/vrrp/aggregateport/spanning-tree` | P0 |
| 5 | 路由邻居不稳 | `show ip ospf neighbor` | P0 |
| 6 | 防火墙策略漏洞 | `display policy` 人工 review | P0 |
| 7 | 弱口令/默认账号 | 设备审计 | P1 |
| 8 | 公网暴露的设备 Web | `display http server` / `show ip http server` | P0 |
| 9 | 未关闭的测试配置 | 人工 review | P1 |
| 10 | 缺失或过期 license | `display license` | P2 |

把所有发现登记到 `05-变更流程与记录/02-入厂隐患清单.md`，按 P0/P1/P2/P3 排期处理。

---

## 第 8 步：写交接手册

不管有没有人来交接，你都要写一份"如果我明天请假一个月，谁能接手"。

包含：
- 设备清单 + 物理位置
- 拓扑图（更新到最新）
- 账号表（在密码管理器里，不在文档里）
- 日常巡检 SOP
- 常见告警处理流程
- 紧急联系人
- 雷区清单（**别动什么配置**）

推荐用 **Markdown + 内网 Wiki**（Docusaurus / Confluence / 语雀），不要用 Word 散在硬盘里。

---

## 第 9 步：第一次月度回顾

入厂 30 天后做一次完整回顾：

- [ ] 所有设备是否都已纳入监控
- [ ] 所有设备是否已加入自动备份
- [ ] 隐患清单 P0/P1 是否全部处理
- [ ] 文档是否更新到最新
- [ ] 变更流程是否跑通过
- [ ] 团队成员是否都接受过变更流程培训

---

## 第 10 步：按厂商速查与命令详解

> **本章目标**：把日常运维中**最常用的命令按场景分组**，每条都解释清楚是什么、为什么用、结果怎么看。
> 适用：思科 / 华为 / 华三 / 锐捷 / 山石 / 深信服 / 戴尔 / 海康 共 8 大类。

### 厂商命令体系速辨

不同厂商命令风格差异很大，**先认体系，再记命令**：

```mermaid
mindmap
  root((命令体系))
    思科 IOS
      show + do
      enable / conf t
      write mem
    华为 VRP
      display + undo
      system-view
      save
    华三 Comware
      display + undo
      system-view
      save
    锐捷 RGOS
      show + no
      configure terminal
      write
    山石 Hillstone
      show + no
      configure
      write
    深信服 Sangfor
      show + no
      configure terminal
      write
```

**快速记忆口诀**：
- 思科 / 锐捷：英文 `show` / `configure terminal` / `write`
- 华为 / 华三：英文 `display` / `system-view` / `save`
- 防火墙类（山石/华为防火墙/华三防火墙）：和路由器类似，但多了 `zone` / `policy` / `session` 这类安全概念

---

### 场景 1：登录与基础查看

> **目的**：确认设备可达、看到基本信息、判断设备角色

#### 1.1 登录设备

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `enable`<br>`conf t` | `enable` 进入特权模式（需要 enable 密码）<br>`conf t` = `configure terminal`，进入全局配置模式 |
| 华为 | `system-view` | 简写 `sys`，从用户视图进入系统视图（才能改配置） |
| 华三 | `system-view` | 同华为 |
| 锐捷 | `enable`<br>`configure terminal` | 简写 `conf t`，进入全局配置模式 |
| 山石 | `configure terminal` | 简写 `conf t` |
| 深信服 | `enable`<br>`configure terminal` | 类似思科 |

> **关键点**：配置模式下才能改东西。用户视图（默认）只能看。

#### 1.2 看版本与设备信息

| 厂商 | 命令 | 输出说明 |
|------|------|---------|
| 思科 | `show version` | 固件版本、运行时间、内存、接口数量、序列号、模块信息 |
| 华为 | `display version` | 同上，但格式不同，包含 VRP 版本 |
| 华三 | `display version` | 同华为 |
| 锐捷 | `show version` | 类似思科 |
| 山石 | `show version` | 类似思科 |
| 深信服 | `show version` | 类似思科 |

> **看到重点**：固件版本（决定有没有已知漏洞）、运行时间（判断设备稳定性）、内存大小。

#### 1.3 看当前时间

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `show clock` | 显示设备本地时间 |
| 华为 | `display clock` | 同 |
| 华三 | `display clock` | 同 |
| 锐捷 | `show clock` | 同 |
| 山石 | `show clock` | 同 |

> **运维第一件事**——对时！日志分析、告警排查都依赖时间准确。差太多要先配 NTP。

---

### 场景 2：接口与 VLAN 排查

> **目的**：定位物理链路、VLAN 划分、错包丢包

#### 2.1 看接口 IP 简表

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show ip interface brief` | 每个接口的 IP / 状态（up/down） |
| 华为 | `display ip interface brief` | 同 |
| 华三 | `display ip interface brief` | 同 |
| 锐捷 | `show ip interface brief` | 同 |
| 山石 | `show ip interface` | 输出格式略不同 |

> **重点看**：`up/up`（物理+协议都通）vs `up/down`（物理通协议不通，比如 ACL 拦了）vs `down/down`（物理就断了）

#### 2.2 看接口详细信息

| 厂商 | 命令 | 重点看什么 |
|------|------|----------|
| 思科 | `show interface Gi0/1`<br>`show interface status` | CRC 错误、丢包、速率、双工模式 |
| 华为 | `display interface GigabitEthernet 0/0/1`<br>`display interface brief` | 同上 + last 300 秒流量 |
| 华三 | `display interface GigabitEthernet1/0/1` | 同华为 |
| 锐捷 | `show interface gigabitEthernet 0/1` | 同思科 |
| 山石 | `show interface ethernet0/0` | 同思科 |

> **CRC 错误** = 网线/模块故障
> **input errors** 持续增长 = 入方向有问题
> **output drops** = 出口拥塞（QoS 没配或带宽不够）

#### 2.3 看 VLAN 划分

| 厂商 | 命令 | 输出 |
|------|------|------|
| 思科 | `show vlan brief` | VLAN 号、名称、接口 |
| 华为 | `display vlan` | 同 |
| 华三 | `display vlan` | 同 |
| 锐捷 | `show vlan brief` | 同 |
| 山石 | `show vlan` | 同 |

#### 2.4 看 Trunk 链路

| 厂商 | 命令 | 重点 |
|------|------|------|
| 思科 | `show interface trunk` | 哪些 VLAN 透传、native VLAN |
| 华为 | `display port vlan` | 同 |
| 华三 | `display port vlan` | 同 |
| 锐捷 | `show interface trunk` | 同 |

> **排查要点**：两端 Trunk 放行的 VLAN 是否一致？native VLAN 是否一致？不一致就丢包。

---

### 场景 3：路由与转发

> **目的**：看路由表、邻居状态、转发路径

#### 3.1 看路由表

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show ip route` | 路由条目 + 下一跳 + 出接口 + 协议来源 |
| 华为 | `display ip routing-table` | 同 |
| 华三 | `display ip routing-table` | 同 |
| 锐捷 | `show ip route` | 同 |
| 山石 | `show ip route` | 同 |

> **路由代码解释（思科/锐捷）**：
> - `C` = 直连，`S` = 静态，`O` = OSPF，`B` = BGP，`R` = RIP，`D` = EIGRP
>
> **路由代码解释（华为/华三）**：
> - `Direct` = 直连，`Static` = 静态，`OSPF` = OSPF，`BGP` = BGP

#### 3.2 看 OSPF 邻居

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show ip ospf neighbor` | 邻居 ID、状态、接口、邻居存活时间 |
| 华为 | `display ospf peer` | 同 |
| 华三 | `display ospf peer` | 同 |
| 锐捷 | `show ip ospf neighbor` | 同 |

> **状态解读**：
> - `Full` / `Full/DR` / `Full/BDR` = 正常
> - `2-Way` = 只收到 Hello，没交换 LSA
> - `Down` = 邻居失效
> - `Init` / `Attempt` / `Exstart` / `Exchange` / `Loading` = 正在建立过程

#### 3.3 看 BGP 邻居

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show ip bgp summary` | 邻居 IP、AS 号、状态、消息收发数 |
| 华为 | `display bgp peer` | 同 |
| 华三 | `display bgp peer` | 同 |
| 锐捷 | `show ip bgp summary` | 同 |

> **BGP 状态**：`Established` 正常。数字代码 `1` = Idle，`2` = Connect，`3` = Active，`4` = OpenSent，`5` = OpenConfirm，`6` = Established。

---

### 场景 4：冗余协议

> **目的**：确认 HSRP/VRRP/堆叠/聚合都健康

#### 4.1 HSRP / VRRP

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show standby brief` | HSRP 组、虚拟 IP、主备角色、优先级 |
| 华为 | `display vrrp brief` | VRRP 组、主备、优先级 |
| 华三 | `display vrrp brief` | 同 |
| 锐捷 | `show vrrp brief` | 同 |

> **主备状态解读**：`Master` 活跃转发流量，`Backup` 待机。一旦主设备 down，备设备应在 3 秒内升级为主。

#### 4.2 堆叠 / CSS / iStack

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show stack` / `show switch` | 堆叠成员、角色、状态 |
| 华为 | `display stack` / `display css status` | CSS / iStack 状态 |
| 华三 | `display irf` / `display irf topology` | IRF（华三的堆叠） |
| 锐捷 | `show stack` | 堆叠信息 |

> **异常告警**：堆叠分裂（两个 Master）是严重的网络隐患，必须立即处理。

#### 4.3 链路聚合

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show etherchannel summary` | 聚合组、成员、协议（LACP/PAgP） |
| 华为 | `display eth-trunk` | 聚合组、成员、状态 |
| 华三 | `display link-aggregation summary` | 同 |
| 锐捷 | `show aggregateport summary` | 同 |

> **两端要一致**：成员口数量、模式（active/passive）必须匹配，否则聚合不起来。

#### 4.4 生成树

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show spanning-tree`<br>`show spanning-tree root` | VLAN 根桥、根端口、指定端口 |
| 华为 | `display stp` | 同 |
| 华三 | `display stp` | 同 |
| 锐捷 | `show spanning-tree` | 同 |

> **根桥规划**：汇聚/核心应主动设低优先级（0 或 4096），让根桥稳定。

---

### 场景 5：安全与 ACL

> **目的**：看策略、看会话、定位丢包

#### 5.1 看 ACL 规则

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show ip access-lists` | ACL 编号、规则、命中数（hit count） |
| 华为 | `display acl all` | 同 |
| 华三 | `display acl` | 同 |
| 锐捷 | `show ip access-list` | 同 |

> **命中次数 0 的规则可能是死规则**——定期清理。

#### 5.2 看 NAT（路由器 / 网关）

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show ip nat translations` | 当前 NAT 转换表 |
| 华为 | `display nat session` | 同 |
| 华三 | `display nat session` | 同 |
| 锐捷 | `show ip nat translation` | 同 |

> **session 数量异常多** = 可能有攻击或泄漏。看下老化时间。

#### 5.3 看防火墙策略（山石 / 华为防火墙 / 华三防火墙）

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 山石 | `show policy` | 策略条目、源/目地址、动作 |
| 华为 FW | `display security-policy rule` | 同 |
| 华三 FW | `display policy interzone` | 同 |

> **防火墙第一看默认策略**：默认 deny 还是默认 permit？这决定了"漏规则"是通还是断。

#### 5.4 看会话表（防火墙 / 状态检测）

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 山石 | `show session` | 当前活跃连接 |
| 华为 FW | `display session` | 同 |
| 华三 FW | `display session` | 同 |

> **session 满了 = 拒绝新连接**。要监控。

---

### 场景 6：性能与日志

> **目的**：判断设备健康度、定位异常

#### 6.1 看 CPU / 内存

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show processes cpu`<br>`show memory statistics` | 进程级 CPU、内存总量与剩余 |
| 华为 | `display cpu-usage`<br>`display memory-usage` | 总体 CPU、内存（百分比） |
| 华三 | `display cpu-usage`<br>`display memory-usage` | 同华为 |
| 锐捷 | `show cpu`<br>`show memory` | 同思科 |
| 山石 | `show cpu`<br>`show memory` | 同 |

> **判断标准**：
> - CPU < 70% 正常
> - 70-85% 关注
> - > 85% 告警，可能有攻击或环路
> - 内存类似

#### 6.2 看日志

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show logging` | 系统日志 |
| 华为 | `display logbuffer` | 缓冲区日志 |
| 华三 | `display logbuffer` | 同 |
| 锐捷 | `show log` | 同 |
| 山石 | `show log` | 同 |

> **生产环境一定要配 syslog 服务器**——本地日志会被覆盖。

#### 6.3 看温度 / 风扇 / 电源

| 厂商 | 命令 | 看到什么 |
|------|------|---------|
| 思科 | `show environment`<br>`show inventory` | 温度、电源、风扇 |
| 华为 | `display temperature` / `display fan` / `display power` | 分别查看 |
| 华三 | `display fan` / `display power` / `display temperature` | 分别 |
| 锐捷 | `show environment` | 综合 |
| 山石 | `show environment` | 综合 |

---

### 场景 7：保存与备份

> **目的**：改完配置不丢、留底能回滚

#### 7.1 保存当前配置

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `write memory`<br>或 `copy running-config startup-config` | running（运行中）→ startup（重启后加载） |
| 华为 | `save` | 同 |
| 华三 | `save` | 同 |
| 锐捷 | `write` | 同 |
| 山石 | `write` | 同 |

> **改完配置不 `save` / `write` = 改了个寂寞**，重启就没了！

#### 7.2 备份配置到 TFTP

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `copy running-config tftp://x.x.x.x/file` | 上传到 TFTP 服务器 |
| 华为 | `tftp x.x.x.x put vrpcfg.zip` | 同 |
| 华三 | `tftp x.x.x.x put startup.cfg` | 同 |
| 锐捷 | `copy running-config tftp://x.x.x.x/file` | 同 |
| 山石 | `tftp x.x.x.x put startup.cfg` | 同 |

#### 7.3 备份配置到 FTP / SFTP

```bash
# 思科
copy running-config ftp://user:pass@x.x.x.x/file
# 华为
ftp x.x.x.x
put vrpcfg.zip
# 华三
<FW> ftp 192.168.1.100
put startup.cfg
```

---

### 场景 8：设备重启与恢复

> **目的**：知道怎么重启、怎么恢复出厂，**用之前一定确认过备份**！

#### 8.1 软重启

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `reload` | 提示保存，按 y 确认 |
| 华为 | `reboot` | 提示确认 |
| 华三 | `reboot` | 同 |
| 锐捷 | `reload` | 同 |
| 山石 | `reload` | 同 |

> **重启会断业务**，必须提前通知 + 选维护窗口 + 准备 Console 救命。

#### 8.2 恢复出厂

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `write erase`<br>`reload` | 删 startup，重启加载空配置 |
| 华为 | `reset saved-configuration`<br>`reboot` | 同 |
| 华三 | `reset saved-configuration`<br>`reboot` | 同 |
| 锐捷 | `write erase`<br>`reload` | 同 |
| 山石 | `reset saved-configuration`<br>`reboot` | 同 |

> ⚠️ **恢复出厂 = 设备清零**。所有配置包括 IP 都会没。**必须**有 Console 救命 + 离线备份。

---

### 场景 9：抓包与诊断

> **目的**：网络不通时定位是 L1/L2/L3/L4/L7 哪一层的问题

#### 9.1 设备本地抓包

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `monitor capture CAP interface Gi0/1 both`<br>`monitor capture CAP start`<br>`show monitor capture CAP buffer` | 抓包到内存，看 buffer |
| 华为 | `capture-packet interface GigabitEthernet 0/0/1 destination file` | 抓包到文件，导出分析 |
| 华三 | `packet-capture interface GigabitEthernet1/0/1` | 抓包 |
| 锐捷 | `capture start interface gi0/1`<br>`capture stop`<br>`capture export tftp ...` | 抓包并导出 |
| 山石 | `debug packet interface ethernet0/0` | debug 抓包 |

> **抓包影响性能**，用完立即关。

#### 9.2 远程 ping / traceroute

| 厂商 | 命令 | 解释 |
|------|------|------|
| 思科 | `ping x.x.x.x`<br>`traceroute x.x.x.x` | 测试连通性 + 路径 |
| 华为 | `ping -a <源IP> x.x.x.x`<br>`tracert x.x.x.x` | 同 |
| 华三 | `ping -a <源IP> x.x.x.x`<br>`tracert x.x.x.x` | 同 |
| 锐捷 | `ping x.x.x.x source <源IP>` | 同 |
| 山石 | `ping x.x.x.x` | 同 |

> **带源 IP 测**非常重要——很多情况下从不同源测结果不同，能定位策略或路由问题。

---

### 场景 10：服务器类（戴尔 R740）

> 这不是网络设备，但运维也要会

| 任务 | 命令（Windows） | 命令（Linux） |
|------|----------------|---------------|
| 看网络 | `ipconfig /all` | `ip a` |
| 看路由 | `route print` | `ip route` |
| 看端口 | `netstat -an` | `ss -tlnp` |
| 看进程 | `tasklist` / `wmic process` | `ps aux` |
| 看 CPU | `typeperf` / 任务管理器 | `top` / `htop` |
| 看内存 | `tasklist` | `free -h` |
| 看磁盘 | `wmic logicaldisk` | `df -h` |
| 看服务 | `sc query` | `systemctl list-units` |
| 看事件 | `eventvwr` / `wevtutil` | `journalctl` |
| 远程 | `mstsc /v:<IP>` | `ssh user@<IP>` |

#### iDRAC（带外管理）核心操作

```
登录：https://<iDRAC IP>
默认账号：root
默认密码：机器标签上
登录后第一件事：改密码！

常用入口：
  Dashboard > System > Properties        # 硬件清单
  Hardware > Health                       # 健康状态
  System > Event Log                      # 硬件日志
  Configuration > System Settings         # 各项配置
  Virtual Console                         # 远程 KVM
  Maintenance > SupportAssist             # 一键收集日志
```

---

### 场景 11：安全/终端类（深信服 / 海康）

#### 11.1 深信服 EDR

**Web 控制台**（主要操作界面）：
```
https://<管理服务器IP>
默认账号：admin
```

**常用模块**：
| 模块 | 路径 | 看什么 |
|------|------|--------|
| 资产 | 资产管理 > 终端列表 | 客户端在线率 |
| 告警 | 事件管理 > 病毒事件 | 病毒查杀记录 |
| 漏洞 | 漏洞管理 > 扫描 | 高危漏洞数 |
| 策略 | 策略管理 > 病毒策略 | 策略是否生效 |
| 远程 | 终端管理 > 远程终端 | 远程 SSH/RDP |

#### 11.2 海康摄像头/NVR

**Web 登录**（摄像头/NVR 本体）：
```
http://<摄像头IP>     # 默认 80 端口
http://<NVR IP>       # NVR 同
```

**核心概念**：
| 概念 | 含义 |
|------|------|
| **信道 / Channel** | NVR 上的一个摄像头位 |
| **码流** | 视频流，主码流（高清录像）+ 子码流（预览） |
| **ONVIF** | 通用协议，让 NVR/平台能发现摄像头 |
| **Hik-Connect** | 海康云端服务（**慎开**，增加攻击面） |

**常见故障**：
| 现象 | 排查 |
|------|------|
| 摄像头离线 | ping → 看电源 → 看 NVR/交换机端口 |
| 录像丢失 | 查磁盘容量 → 查录像计划 → 查时间 |
| 回放黑屏 | 重启 NVR → 重启摄像头 → 重新添加 |
| 客户端看不到 | 平台对账号授权 → ONVIF 启用 → 密码对 |

---

### 速查速记：10 条"救命命令"

> 真的什么都看不下去、什么都进不去的时候，**这 10 条 + Console** 能救命：

```bash
# 1. 看是不是活着
ping <设备IP>

# 2. 走 Console 救命
# 用 MobaXterm / SecureCRT，串口设置 9600 8N1

# 3. 看现在什么状态（华为/华三为例）
display cpu-usage
display memory-usage
display interface brief

# 4. 看时钟
display clock

# 5. 看接口物理状态
display interface <接口名>

# 6. 看路由
display ip routing-table

# 7. 看邻居（OSPF）
display ospf peer

# 8. 看 VRRP
display vrrp brief

# 9. 软重启
reboot

# 10. 恢复出厂（最后手段）
reset saved-configuration
reboot
```

---

## 附录 A：推荐软件清单

### A.1 终端/连接类

| 软件 | 平台 | 免费 | 备注 |
|------|------|------|------|
| **MobaXterm** | Windows | 个人版免费 | SSH/串口/文件传输全能，强烈推荐 |
| Xshell 7 | Windows | 家庭/学校免费 | 商业功能要付费 |
| SecureCRT | 全平台 | 付费 | 老牌稳定，习惯的人多 |
| Termius | 全平台 | 个人版免费 | 跨平台 |
| PuTTY | Windows | 免费 | 极简，老古董 |
| Windows Terminal | Windows | 免费 | Windows 11 自带 |
| iTerm2 | macOS | 免费 | macOS 首选 |
| Tabby | 全平台 | 免费 | 新一代，颜值高 |

### A.2 抓包/诊断

| 软件 | 用途 | 备注 |
|------|------|------|
| **Wireshark** | 抓包分析 | 必备 |
| tcpdump | 命令行抓包 | Linux 服务器 |
| ping / traceroute / mtr | 路径追踪 | 内置 |
| nmap | 端口扫描 | 排查用，别乱扫生产 |
| iperf3 | 带宽测试 | 测链路质量 |
| BestTrace | 路由追踪 | 有中国地图展示 |

### A.3 配置管理

| 软件 | 用途 | 备注 |
|------|------|------|
| **Oxidized** | 自动备份 + diff | 开源，强推 |
| RANCID | 自动备份 | 老牌 |
| NetBox | IP 资产 + 拓扑 | 开源，自建 |
| Git | 配置版本管理 | 配 Oxidized 用 |

### A.4 监控告警

| 软件 | 用途 | 备注 |
|------|------|------|
| **Zabbix** | 综合监控 | 开源，企业首选 |
| LibreNMS | 网络监控 | 开源，自动发现强 |
| Prometheus + Grafana | 监控 + 大屏 | 容器化场景 |
| PRTG | 商业监控 | 颜值高，license 贵 |
| SolarWinds | 商业监控 | 功能强，贵 |
| 华为 eSight | 华为设备管理 | 有华为设备建议装 |
| 锐捷 RIIL | 锐捷设备管理 | 有锐捷设备建议装 |
| 华三 iMC | 华三设备管理 | 有华三设备建议装 |

### A.5 资产管理

| 软件 | 用途 | 备注 |
|------|------|------|
| **NetBox** | IP/DCIM/线缆 | 开源，强推 |
| Snipe-IT | 资产 IT 管理 | 开源 |
| GLPI | 资产 + 工单 | 开源，功能多 |

### A.6 文档协作

| 软件 | 用途 | 备注 |
|------|------|------|
| **语雀 / 飞书文档 / 钉钉文档** | 团队 Wiki | 国内首选 |
| Notion | 团队 Wiki | 国际团队 |
| Confluence | 团队 Wiki | Atlassian 系 |
| Docusaurus | 技术文档 | 开源 |
| MkDocs | 技术文档 | 开源 |
| **Obsidian** | 个人/小团队笔记 | 适合运维手册 |

### A.7 密码管理

| 软件 | 平台 | 备注 |
|------|------|------|
| **Bitwarden** | 全平台 | 开源免费，强推 |
| 1Password | 全平台 | 付费，体验好 |
| KeePass | 全平台 | 老牌，离线 |

### A.8 远程/堡垒机

| 软件 | 用途 | 备注 |
|------|------|------|
| **Jumpserver** | 堡垒机 | 开源，国内最流行 |
| Teleport | 堡垒机 | 开源，新潮 |
| WindTerm | 终端+文件 | 新秀 |
| mstsc | Windows 远程桌面 | 内置 |
| VNC Viewer | 跨平台远程 | 备选 |

### A.9 画图

| 软件 | 用途 | 备注 |
|------|------|------|
| **Draw.io (diagrams.net)** | 通用画图 | 免费，强推 |
| Visio | 专业画图 | 微软，付费 |
| ProcessOn | 在线画图 | 国内，协作 |
| Lucidchart | 在线画图 | 国际，付费 |
| yEd | 自动布局 | 复杂图省力 |
| NetBox 内置拓扑 | DCIM 拓扑 | 自建首选 |

### A.10 提效工具

| 软件 | 用途 |
|------|------|
| **Snipaste** | 截图贴图 |
| Everything | 文件搜索 |
| uTools | 工具集 |
| QuickLook | 空格预览 |
| PowerToys | Windows 工具集 |
| 7-Zip | 解压缩 |
| Notepad++ | 文本编辑 |
| Typora / MarkText | Markdown 编辑 |

---

## 附录 B：常用账户/资产表模板

参见 `06-资产与拓扑/01-资产清单模板.md`

---

## 附录 C：变更流程与回滚模板

参见 `05-变更流程与记录/01-变更申请单模板.md`

---

## 几条铁律

> 1. **没有变更方案不动配置**——变更前先写方案、回滚步骤、影响范围
> 2. **没有备份不动配置**——变更前必须备份当前配置
> 3. **核心设备改配置必须有第二人在场**——能多一双眼睛救命
> 4. **变更在窗口期做**——别在业务高峰期动核心设备
> 5. **改完配置观察 30 分钟再走**——出问题了人还在
> 6. **一切操作留痕**——命令、截图、时间、结果都要记
> 7. **不懂就问，别猜**——猜错一次比问十次代价都大
> 8. **把账号密码交给密码管理器，别交给运气**
